#Write a program to remove a given item from the set.

data={'My', 'Name', 'Is', 'Umanshi'}
itemdata='item'
for i in data:
    if i==item:
        data.remove(item)
            
else:
    print("Item not found")